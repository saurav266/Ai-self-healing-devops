apiVersion: v1
kind: Service

metadata:
  name: self-healing-node-service
  namespace: self-healing

spec:
  type: ClusterIP

  selector:
    app: self-healing-node-app

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http